# AM 20/Jul/26 - Presence of this file at the project root puts this directory
# on sys.path for pytest, so tests can "from app... import" regardless of the
# working directory the test runner (terminal, VS Code, IDE) launches from.
